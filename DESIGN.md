# NOD 디자인 시스템

사용자가 제공한 **Creative Mind Map** 랜딩과 **Productivity Canvas** 앱 HTML을 기준으로 작성한 디자인 가이드다. 두 화면의 공통 시각 언어를 NOD에 적용한다. 실제 화면 구현과 시각 검증은 별도 작업이다.

## 1. 디자인 방향

따뜻한 종이 위에 손으로 그린 듯한 개인 링크 보관함. 크림 바탕, 머스터드 옐로, 둥근 글자, 불규칙한 테두리와 살짝 어긋난 그림자로 친근함을 만든다. 민트·라벤더·코랄·세이지는 작은 영역에 보조색으로 사용한다.

- 랜딩: 넓은 여백, 큰 제목, 유기적인 도형, 기울어진 이미지 프레임으로 개성을 드러낸다.
- 앱: 같은 색상과 테두리를 쓰되 검색·링크 제목·저장 날짜를 빠르게 읽을 수 있게 정렬한다.
- 손그림 느낌은 테두리와 장식에서 만든다. 본문 텍스트 자체를 흔들거나 과하게 회전시키지 않는다.
- 제공된 레퍼런스가 스타일의 기준이다. 일반적인 미니멀 UI나 다른 스킬의 기본 취향으로 교체하지 않는다.

## 2. 색상

### 원본 팔레트

| 토큰 | 값 | 역할 |
| --- | --- | --- |
| `color.creme` | `#FFFDFA` | 기본 페이지 배경 |
| `color.white` | `#FFFFFF` | 카드, 입력, 떠 있는 패널 |
| `color.ink` | `#4A4A4A` | 본문, 제목, 손그림 테두리 |
| `color.brand` | `#E8B931` | 주요 버튼 배경, 강조 장식, 활성 영역 |
| `color.mint` | `#E0F2F1` | 보조 버튼, 장식 배경 |
| `color.lavender` | `#F3E5F5` | 보조 카드와 아이콘 배경 |
| `color.coral` | `#FF8A80` | 따뜻한 보조 강조 |
| `color.sage` | `#8BA888` | 연결 안내, 보조 액션 강조 |
| `color.soft-grey` | `#F2F2F2` | 중립 표면, 아이콘 슬롯 |

### 의미별 사용 규칙

| 토큰 | 값 | 적용 |
| --- | --- | --- |
| `surface.page` | `color.creme` | 페이지 전체 |
| `surface.card` | `color.white` | 읽기 영역과 카드 |
| `surface.subtle` | `color.soft-grey` | 보조 컨트롤 |
| `surface.selected` | 브랜드색 20% + 페이지 배경 | 선택된 탐색 항목 |
| `text.primary` | `color.ink` | 제목과 본문 |
| `text.secondary` | `#6B665C` | 메타데이터, 설명. 가독성을 위한 보완값 |
| `text.accent` | `#80620B` | 밝은 바탕 위 작은 강조 텍스트. 보완값 |
| `action.primary.bg` | `color.brand` | 주요 행동 |
| `action.primary.text` | `color.ink` | 노란 버튼 위 글자 |
| `border.strong` | `color.ink` | 손그림 테두리 |
| `border.subtle` | 잉크색 20% | 비상호작용 구획 |
| `focus.ring` | `color.ink` | 키보드 포커스 |

브랜드 노랑 위 흰색 작은 글자와 밝은 배경 위 노란 본문은 원본 그대로 적용하지 않는다. 색상 투명도는 장식과 구획에 주로 사용하고, 필수 텍스트에는 명시적인 색상을 쓴다. 포커스·입력 경계는 옅은 장식 테두리만으로 표현하지 않는다.

상태색은 원본의 초록·노랑·빨강 구분을 따른다. 초기 제안은 성공 `#DCFCE7 / #15803D`, 주의 `#FEF9C3 / #854D0E`, 오류 `#FEE2E2 / #B91C1C`의 배경/텍스트 조합이다. 아이콘과 문구를 함께 표시한다. 코랄과 세이지 자체를 오류·성공 텍스트로 사용하지 않는다.

## 3. 타이포그래피

| 역할 | 원본 서체 | 기준 |
| --- | --- | --- |
| 브랜드·영문 제목 | Fredoka | 500–700, 둥글고 부드러운 인상 |
| 영문 본문·UI | Quicksand | 400–700, 편안한 읽기 |
| 한글 제목 | 미확정 | Fredoka와 어울리는 둥근 서체를 실제 문장으로 비교 |
| 한글 본문·UI | 미확정 | 작은 메타데이터와 긴 링크 제목의 가독성 우선 |

한글 서체 선정 전에는 시스템 한글 고딕체를 대체 서체로 사용한다. 레퍼런스의 `font-black`은 요청한 웹폰트 범위 300–700을 넘으므로 실제 굵기는 700을 기준으로 조정한다.

| 텍스트 | 모바일 / 넓은 화면 | 행간 |
| --- | --- | --- |
| 랜딩 대표 제목 | 48–60 / 72–96px | 1.1–1.2 |
| 랜딩 섹션 제목 | 36 / 48–60px | 1.2 |
| 앱 페이지 제목 | 32–40 / 40–48px | 1.2 |
| 컴포넌트 제목 | 20–24px | 1.3 |
| 링크 제목 | 18px | 1.4 |
| 본문 | 16–20px | 1.6 |
| 메타데이터·보조 문구 | 12–14px | 1.5 |

원본의 10px 레이블은 필수 정보에 사용하지 않는다. 긴 링크 제목은 모바일에서 두 줄까지 허용하고 날짜와 출처가 눌리지 않게 배치한다. 강조 밑줄은 물결 모양과 민트/세이지를 사용하되 글자와 겹치지 않는다.

## 4. 형태와 그림자

| 토큰 | 값 |
| --- | --- |
| `radius.control` | `12px` |
| `radius.panel` | `24px` |
| `radius.creative` | `32px` |
| `radius.pill` | `999px` |
| `radius.organic` | `60% 40% 30% 70% / 60% 30% 70% 40%` |
| `radius.blob` | `30% 70% 70% 30% / 30% 30% 70% 70%` |
| `radius.doodle` | `255px 15px 225px 15px / 15px 225px 15px 255px` |
| `radius.doodle-thick` | `255px 25px 225px 25px / 25px 225px 25px 255px` |
| `border.doodle` | `2px solid #4A4A4A` |
| `border.doodle-emphasis` | `3px solid #4A4A4A` |
| `shadow.sketch` | `4px 4px 0 rgba(74, 74, 74, 0.1)` |
| `shadow.sketch-large` | `8px 8px 0 rgba(0, 0, 0, 0.05)` |
| `shadow.sketch-hover` | `6px 6px 0 #E8B931` |

카드와 주요 CTA에는 손그림 모서리를 사용한다. 입력과 작은 조작 요소에는 일반 모서리를 쓸 수 있다. 점선은 안내·보조 영역에 한정하고, 발광보다 평면적인 스케치 그림자를 우선한다.

장식 카드의 기울기는 ±1–1.5°, 큰 이미지 프레임은 ±3°, 작은 메모는 최대 ±6°. 입력·목록 본문·중요 조작은 수평을 기본으로 한다.

## 5. 여백과 레이아웃

기본 간격은 `4, 8, 12, 16, 24, 32, 40, 48, 64, 96, 128px`.

- 좌우 여백은 모바일 24px, 데스크톱 40px부터 시작한다.
- 랜딩 최대 폭 1280px, 앱 본문 1152px. 카드 내부는 24px, 설명 카드는 40px 여백을 기준으로 한다.
- 랜딩은 12열, 히어로는 글과 이미지 7:5 비율. 좁은 화면에서는 세로로 쌓는다.
- 사이드바를 채택하면 폭은 256px. 화면 수가 적은 Phase 1에 억지로 도입하지 않는다.
- 640 / 768 / 1024px을 초기 전환점으로 사용하고 실제 내용에 맞춰 조정한다.
- 모바일에서 탐색을 숨기면 동일한 조작을 제공하는 메뉴를 둔다.
- 고정 헤더와 하단 안내가 본문·마지막 행·포커스 대상을 가리지 않게 한다. 높이 고정으로 내용을 자르지 않는다.

원본 앱의 도트 배경은 20px 주기이며 두 번째 레이어를 10px 이동한 작은 점이다. 낮은 불투명도로 배경에만 사용하고 카드와 입력면은 단색으로 유지한다.

## 6. 컴포넌트

| 요소 | 스타일과 동작 |
| --- | --- |
| 주요 버튼 | 노란 배경, 차콜 글자, 굵은 손그림 테두리. hover는 차콜 배경과 흰 글자 또는 그림자 변화 |
| 보조 버튼 | 흰색이나 민트 배경, 차콜 글자, 일반 또는 손그림 테두리 |
| 아이콘 버튼 | 20–24px 아이콘, 조작 영역은 원칙적으로 44px 이상. 용도를 설명하는 접근성 이름 |
| 검색·입력 | 흰 배경, 명확한 경계, 명시적 레이블. focus는 차콜 2px 외곽선과 2px 간격 |
| 탐색 항목 | 아이콘과 글자. 선택 시 노란색 20% 배경, 점선 테두리, 짙은 글자 |
| 링크 행 | 흰 배경, 절제된 손그림 테두리, 제목·출처·URL·저장 날짜·조작. hover로 경계 강조 |
| 안내 카드 | 흰 배경, 24–40px 여백, 스케치 그림자. 장식 아이콘은 연한 유기형 위에 배치 |
| 배지 | 알약 형태, 짧은 글자, 필요하면 상태 아이콘. 색상만으로 의미를 전달하지 않음 |
| 시작 안내 | 세이지 계열 경계와 연한 배경, 짧은 안내, 연결 CTA, 닫기 |
| 오류·재시도 | 연한 빨간 영역, 구체적인 오류 설명, 재시도 버튼 |

기본·hover·focus-visible·pressed·disabled 상태를 정의한다. 비동기 조작은 loading과 성공/실패 상태를 갖는다. disabled와 loading에서 레이아웃이 흔들리지 않게 한다.

레퍼런스의 지표 카드와 빠른 실행은 시각 패턴으로 취급한다. 빈 공간을 채우려고 Phase 1에 없는 지표나 기능을 추가하지 않는다.

## 7. 아이콘과 브랜드

시스템 아이콘은 원본의 **Material Symbols Outlined**를 사용한다. 기본 24px, 보조 20px, 장식 32–48px. 선 굵기와 fill 사용을 화면 전체에서 통일한다.

원본에 전구와 팔레트가 혼재하므로 정식 NOD 로고는 미확정이다. NOD 이름과 노란 유기형을 참고하되 어느 아이콘도 정식 로고로 단정하지 않는다. 이미지 URL·인물 이미지·인용문은 참고 소재이며 실제 제품 자산으로 확정되지 않았다.

## 8. 모션

- hover는 150–300ms. 작은 그림자·색상 변화가 기본이며 카드 이동은 최대 4px.
- 랜딩 장식에는 원본의 6초 주기 부유(최대 20px, 2°)를 선택적으로 적용한다.
- 링크 목록과 작업 화면에는 상시 부유를 넣지 않는다.
- `prefers-reduced-motion: reduce`에서 부유·회전·이동을 끄고 상태는 정적으로 전달한다.
- 터치 조작은 hover에 의존하지 않는다. 테두리나 크기 변화로 레이아웃이 밀리지 않게 한다.

## 9. NOD 적용 범위

Phase 1은 **URL·제목·출처·저장 시각을 다루는 개인 링크 라이브러리**다. Google 로그인, 확장 저장, 목록, 검색, 원문 열기, 삭제, 확장 연결 관리를 지원한다.

AI Insights·AI 요약·의미 검색·태그·공동 편집·과금·사용량 제한·가상의 사용자 수는 제공 HTML의 참고 문구이며 제품 사양이 아니다. 링크별 Completed/Pending/Failed도 현재 제품 데이터에 추가하지 않는다. 썸네일은 필수로 요구하지 않는다.

UI 문구는 현재 **한국어 웹앱**에 맞추고 구체적인 행동을 전달한다. 씨앗 심기 같은 비유는 소개문에서 절제해 사용하고, 조작은 저장·검색·다시 시도처럼 직접적인 단어로 표현한다. 확장 설정을 포함한 전체 언어 통일은 별도 결정한다.

필요한 상태: 로그인 확인 중, 로딩, 첫 저장 전 빈 화면, 검색 결과 없음, 조회 실패와 재시도, 삭제 확인과 성공/실패, 확장 연결 상태, 연결 해제 처리, 세션 만료.

## 10. 구현·확인 기준

- HTML·CSS·JavaScript 분리를 유지한다. 참고 HTML의 Tailwind CDN은 필수가 아니며 토큰은 CSS custom properties로 대응한다.
- 이번 범위는 라이트 테마다. 다크 테마는 미설계 상태다.
- 일반 글자 4.5:1, 큰 글자 3:1, 필요한 UI 경계와 상태 표시 3:1을 확인 기준으로 삼는다. 제안한 조합은 실제 렌더링에서 검증한다.
- 320px 화면, 긴 한글·영문 제목, 200% 확대, 키보드 조작, 동작 줄이기 설정을 확인한다.
- 랜딩·라이브러리·빈 화면·오류·확장 설정에서 색상·글자 위계·경계·조작 상태의 일관성을 확인한다.
- 이 문서는 제공 코드에서 추출한 사양이며 브라우저 검증을 마친 구현을 뜻하지 않는다.

## 근거와 미결정 사항

스타일 원본: 사용자가 제공한 두 HTML(Creative Mind Map / Productivity Canvas). 제품 범위는 [docs/decisions.md](docs/decisions.md), 배경은 [docs/design-context.md](docs/design-context.md)를 참조한다.

미결정: 한글 서체, 정식 로고, 실제 제품 이미지, 전체 언어 정책. 의미별 보완색과 작은 글자 크기는 원본의 인상을 유지하면서 가독성을 보완하는 이 가이드의 제안값이다.
