# NOD extension options UI kit

Recreation of `apps/nod/extension/options.html` restyled per DESIGN.md. Copy verbatim (English — the options screen is English in the current source; language policy undecided).

- `index.html` — interactive: save origin, connect/disconnect (fake), last activity status.
- `Options.jsx` — composes TextInput, Button, Badge, StatusMessage, Brand.
