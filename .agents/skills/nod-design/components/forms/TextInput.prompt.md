White text/search field with an explicit label, 48px tall, 12px radius; hover/focus switch to the 2px charcoal border and a 2px offset focus ring.

```jsx
<TextInput label="저장한 링크 검색" hideLabel icon="search" placeholder="제목이나 출처로 검색" />
<TextInput label="NOD service origin" type="url" hint="Use HTTPS" />
<TextInput label="URL" error="주소 형식을 확인해 주세요." />
```
