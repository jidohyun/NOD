Static grey placeholder row used while the link list loads (3 rows typical). No motion by default.

```jsx
<ul style={{display:"grid",gap:10,padding:0}}><SkeletonRow /><SkeletonRow /><SkeletonRow /></ul>
```
