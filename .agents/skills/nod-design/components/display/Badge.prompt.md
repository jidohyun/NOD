Pill label for short status text; status tones carry an icon so meaning isn't color-only.

```jsx
<Badge tone="success">연결됨</Badge>
<Badge tone="error">저장 실패</Badge>
<Badge tone="mint">Chrome</Badge>
```
