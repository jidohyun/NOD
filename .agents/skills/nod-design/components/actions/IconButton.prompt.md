Square 44px icon-only control (search submit, logout, delete) with a required accessible label.

```jsx
<IconButton icon="search" label="검색" />
<IconButton icon="delete" label="“제목” 삭제" tone="danger" />
```
Hover shows the doodle border; danger tone tints red on hover.
