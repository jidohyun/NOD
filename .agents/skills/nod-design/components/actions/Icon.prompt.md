Material Symbols Outlined glyph wrapper; use for every system icon (never hand-drawn SVG or emoji).

```jsx
<Icon name="search" />
<Icon name="delete" size={20} label="삭제" />
```
Sizes: 24 default, 20 secondary, 32–48 decorative. Keep weight/fill consistent across a screen (wght 400, FILL 0).
