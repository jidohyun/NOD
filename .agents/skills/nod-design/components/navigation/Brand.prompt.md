NOD logo link. Wordmark (black "NOD" + gold check mark) on light surfaces; `variant="mark"` for the square icon.

```jsx
<Brand />
<Brand variant="mark" size={40} />
```
Dark-background wordmark: `assets/nod-logo-dark-bg.png`.
