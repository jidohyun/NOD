Icon + label navigation row; selected state uses the 20% yellow tint with a dashed charcoal border.

```jsx
<NavItem icon="bookmarks" label="저장한 링크" selected />
<NavItem icon="extension" label="확장 프로그램" />
```
Only adopt a 256px sidebar if the screen count justifies it (DESIGN.md §5).
